@{
    string[] logs = ViewBag.EmployeeLogs;
}

<h1>Employee Logs</h1>

@if (logs != null && logs.Length > 0)
{
    <ul>
        @foreach (var log in logs)
        {
            <li>@log</li>
        }
    </ul>
}
else
{
    <p>No logs available.</p>
}